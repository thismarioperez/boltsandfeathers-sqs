/**
 * @description Declare app wide config variables
 * @member {object} config
 * @memberof core
 */
const config = {
  baseTransition: 200,
  pageTransition: 1000,
  minColumnWidth: 285,
  maxNumberColumns: 3,
  cropImagesSetting: 'horizontal'//"square", "horizontal", "vertical", "none"
};

export default config;