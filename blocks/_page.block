{.var @pageBanner collection.mainImage}

<article{.if @pageBanner}{.or} data-controller="AccountForHeader"{.end}>
  {.if @pageBanner}
    {@pageBanner|apply _banner.block}
  {.end}{.end}
  <section class="cms cms--{collection.id} -wrapper -limitter -space-t -space-b" data-collection-id="{collection.id}">
    {squarespace.main-content}
  </section>
</article>